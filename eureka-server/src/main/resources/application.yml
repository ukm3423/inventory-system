spring:
  application:
    name: eureka-server

server:
  port: 8761
  # address: 192.168.1.157

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
  server:
    enable-self-preservation: false

